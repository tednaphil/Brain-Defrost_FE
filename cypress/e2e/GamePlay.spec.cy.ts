describe('Brain Defrost GamePlay Stories', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000/')
  })
  it('Allows user to start a game', () => {
  })
  it('Allows user to select and submit an answer', () => {
  })
  it('Notifies user if no answer was submitted', () => {
  })
  it('Shows game stats at the end of the game', () => {
  })
  it('Allows user to return to home to generate a new game', () => {
  })
})